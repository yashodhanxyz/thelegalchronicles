{
  "name": "thelegalchronicles",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
